#ifndef CC_TC264_PORT_H
#define CC_TC264_PORT_H

#include <stdint.h>

#include "image.h"
#include "motor4.h"

/*
 * 这些声明是板级适配边界，不假设你使用 iLLD、TASKING 还是 HighTec。
 * 在 TC264 工程中实现它们，然后在主循环/中断里调用即可；核心库本身不访问寄存器。
 */
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_tc264_millis 函数声明
// 返回类型          uint32_t
// 使用示例          cc_tc264_millis(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint32_t cc_tc264_millis(void);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_tc264_camera_read 函数声明
// 返回类型          uint8_t
// 使用示例          cc_tc264_camera_read(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint8_t cc_tc264_camera_read(cc_image_u8_t *frame);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_tc264_motor_write 函数声明
// 返回类型          void
// 使用示例          cc_tc264_motor_write(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
void cc_tc264_motor_write(const cc_motor4_command_t *command);

#endif

