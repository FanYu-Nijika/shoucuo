#ifndef CC_RING_BUFFER_H
#define CC_RING_BUFFER_H

#include <stdint.h>

typedef struct {
    uint8_t *data;
    uint16_t capacity;
    uint16_t head;
    uint16_t tail;
    uint16_t count;
} cc_ring_buffer_t;

//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_init 函数声明
// 返回类型          void
// 使用示例          cc_ring_buffer_init(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
void cc_ring_buffer_init(cc_ring_buffer_t *buffer, uint8_t *storage, uint16_t capacity);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_clear 函数声明
// 返回类型          void
// 使用示例          cc_ring_buffer_clear(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
void cc_ring_buffer_clear(cc_ring_buffer_t *buffer);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_push 函数声明
// 返回类型          uint8_t
// 使用示例          cc_ring_buffer_push(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint8_t cc_ring_buffer_push(cc_ring_buffer_t *buffer, uint8_t value);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_pop 函数声明
// 返回类型          uint8_t
// 使用示例          cc_ring_buffer_pop(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint8_t cc_ring_buffer_pop(cc_ring_buffer_t *buffer, uint8_t *value);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_peek 函数声明
// 返回类型          uint8_t
// 使用示例          cc_ring_buffer_peek(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint8_t cc_ring_buffer_peek(const cc_ring_buffer_t *buffer, uint8_t *value);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_size 函数声明
// 返回类型          uint16_t
// 使用示例          cc_ring_buffer_size(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint16_t cc_ring_buffer_size(const cc_ring_buffer_t *buffer);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_free 函数声明
// 返回类型          uint16_t
// 使用示例          cc_ring_buffer_free(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint16_t cc_ring_buffer_free(const cc_ring_buffer_t *buffer);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_is_empty 函数声明
// 返回类型          uint8_t
// 使用示例          cc_ring_buffer_is_empty(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint8_t cc_ring_buffer_is_empty(const cc_ring_buffer_t *buffer);
//-------------------------------------------------------------------------------------------------------------------
// 函数简介          cc_ring_buffer_is_full 函数声明
// 返回类型          uint8_t
// 使用示例          cc_ring_buffer_is_full(...);
// 备注信息          参数含义请参考同名源文件实现。
//-------------------------------------------------------------------------------------------------------------------
uint8_t cc_ring_buffer_is_full(const cc_ring_buffer_t *buffer);

#endif

